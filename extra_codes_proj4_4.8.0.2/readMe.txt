other.extra edit to the same structure as esri.extra. That is mean, if row start with # it is "Name", next line <"code"> "one space" "proj"

Add same other EPSG codes from other sources
After <"code"> must be a space, if in not need proj def (proj4 know epsg code)